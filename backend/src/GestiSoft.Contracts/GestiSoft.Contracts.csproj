<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Solo per riusare gli enum di dominio (es. RuoloUtente) nei DTO verso il frontend, niente
         altro da Domain: i Contracts restano "forma sul filo", non entità EF. -->
    <ProjectReference Include="..\GestiSoft.Domain\GestiSoft.Domain.csproj" />
  </ItemGroup>

</Project>
